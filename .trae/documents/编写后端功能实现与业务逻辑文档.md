根据您的要求，我制定了以下计划来生成“后端功能实现清单”及“功能需求分析和业务逻辑实现文档”。

# 计划：编写后端功能与业务逻辑文档

## 目标
根据前端接口需求和现有后端代码，产出一份详尽的文档，明确后端的功能实现细节、业务逻辑流转及架构设计。

## 执行步骤

1.  **编写文档内容**
    -   **创建文件**：`后端功能实现与业务逻辑文档.md`
    -   **内容大纲**：
        -   **1. 系统架构概述**
            -   MVC 分层架构说明（Controllers / Services / Models）。
            -   技术栈（FastAPI, Pydantic, DashScope SDK, PIL）。
        -   **2. 功能实现清单**
            -   对应前端需求的后端模块清单。
            -   涵盖：模型管理、多模式生成（Wan/Z-Image）、中英互译、图片管理（缩略图/原图）。
        -   **3. 业务逻辑详解**
            -   **生成流程**：参数校验 -> 服务分发 -> 异步任务轮询 -> 结果下载 -> 数据清洗。
            -   **图片服务**：文件扫描 -> Base64 ID 编解码 -> 智能缩略图生成与缓存。
        -   **4. 接口规范摘要**
            -   列出核心 API 的输入输出定义（对应 Pydantic Model）。
        -   **5. 数据存储与目录结构**
            -   说明 `outputs/` 目录的分层结构及 `.thumbs/` 缓存机制。

2.  **验证与交付**
    -   确认文档内容与当前代码实现（特别是 `images_controller.py` 和 `dashscope_client_service.py`）完全一致。
    -   交付 Markdown 文件。

## 预期产出
-   一个名为 `后端功能实现与业务逻辑文档.md` 的文件，作为项目后端开发的权威参考文档。
